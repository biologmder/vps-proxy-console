services:
  agent:
    image: ghcr.io/biologmder/vps-proxy-console-agent:main
    restart: unless-stopped
    network_mode: host
    env_file: agent.env
    volumes:
      - agent-data:/data
volumes:
  agent-data: {}
